# 部署与发布

本文档聚焦根目录的容器化部署方案，覆盖开发演示、HTTP 直连和 HTTPS 代理三种场景。

## 1. 核心文件

- [docker-compose.yml](../../docker-compose.yml)
- [docker-compose.env.yml](../../docker-compose.env.yml)
- [docker-compose.dev.yml](../../docker-compose.dev.yml)
- [.env.example](../../.env.example)

## 2. Compose 模式

### 2.1 基础依赖模式

```bash
docker compose -f docker-compose.env.yml up -d mysql redis rabbitmq
```

用途：本机联调时只需要基础服务。

### 2.2 开发容器模式

```bash
docker compose -f docker-compose.dev.yml up -d
```

用途：基础服务 + `blog-service` 容器。

### 2.3 完整部署模式

```bash
cp .env.example .env
COMPOSE_PROFILES=http docker compose up -d
```

用途：完整启动 MySQL、Redis、RabbitMQ、`blog-service`、`blog-admin`、`blog-web` 和 HTTP Nginx。

本机没有域名时，HTTP 模式可以直接用 `localhost`：

- 博客前台：`http://localhost`
- 管理后台：`http://localhost:3001`
- 后端 API：`http://localhost:8888`

这种方式不需要修改 hosts。前台必须优先走 `http://localhost`，因为它经过外层 Nginx，`/api` 和 `/uploads` 才会被代理到正确服务。

本地 `.env` 关键配置示例：

```env
COMPOSE_PROFILES=http
SPRING_PROFILES_ACTIVE=docker
SERVER_PORT=8888
BLOG_ADMIN_PUBLISHED_PORT=3001
BLOG_WEB_PUBLISHED_PORT=3002

DB_HOST=localhost
DB_PORT=3306
DB_NAME=blog
DB_USER=blog
DB_PASSWORD=change_me_db_password
DB_ROOT_PASSWORD=change_me_root_password

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PUBLISHED_PORT=6379
REDIS_PASSWORD=
REDIS_DATABASE=0

RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=change_me_rabbitmq_password
RABBITMQ_VHOST=/
```

### 2.4 HTTPS 代理模式

```bash
cp .env.example .env
COMPOSE_PROFILES=https docker compose up -d
```

用途：启用 `nginx-proxy` + `acme-companion` 自动签发证书。

HTTPS 模式需要让外层代理域名和内层 Nginx 路由域名保持一致：

```env
COMPOSE_PROFILES=https
ALL_DOMAINS=admin.example.com,blog.example.com
NGINX_ADMIN_DOMAIN=admin.example.com
NGINX_WEB_DOMAIN=blog.example.com
ACME_EMAIL=admin@example.com
NGINX_PROXY_CONTAINER=swater-nginx-proxy
```

说明：

- `ALL_DOMAINS` 用逗号分隔，给 `nginx-proxy` 和 ACME 申请证书使用
- `NGINX_ADMIN_DOMAIN` 决定哪个域名进入管理后台
- `NGINX_WEB_DOMAIN` 决定哪个域名进入博客前台
- `NGINX_PROXY_CONTAINER` 默认应等于 [docker-compose.yml](../../docker-compose.yml) 中 `nginx-proxy` 服务的 `container_name`

补充说明：

- 如果仓库位于 WSL 文件系统，比如 `/home/...`，请在 WSL 终端里执行 `docker compose`
- 不要在 Windows 侧对 `\\wsl.localhost\...` 路径直接运行 `docker-compose.exe`，否则文件绑定挂载会报 `includes invalid characters for a local volume name`

## 3. 关键环境变量

### 基础服务

- `DB_NAME` / `DB_USER` / `DB_PASSWORD` / `DB_ROOT_PASSWORD`
- `REDIS_PASSWORD`
- `RABBITMQ_USER` / `RABBITMQ_PASSWORD`

### 后端服务

- 后端 profile：完整 Docker 部署由 Compose 命令固定为 `docker`
- `SERVER_PORT`：后端容器内端口和宿主机映射端口，默认 `8888`

### YAML 支持但 Compose 默认不注入的变量

`application-docker.yml` 支持邮件、地图、存储、安全 Token、搜索和 CORS 等环境变量，例如 `MAIL_*`、`AMAP_KEY`、`JWT_SECRET_KEY`、`AUTH_EMAIL_SESSION_SECRET`、`OSS_*`、`QINIU_*`、`SEARCH_*`、`APP_CORS_*`。当前根目录 [docker-compose.yml](../../docker-compose.yml) 只显式传入基础服务连接和 `SERVER_PORT`；如需使用这些变量，需要先把它们加入 `blog-service.environment` 或通过部署平台注入容器环境。

### 管理员初始化

- 首次空库启动时，后端会自动检查并创建管理员账号
- Docker profile 当前来自 [application-docker.yml](../../blog-service/src/main/resources/application-docker.yml)，默认用户名 `admin`，默认密码 `change_me_admin_password`
- 本机 `dev` profile 来自 [application-dev.yml](../../blog-service/src/main/resources/application-dev.yml)，默认用户名 `admin`，默认密码 `admin`
- 管理员用户已存在时不会重置密码
- 生产环境应在首次启动前修改 `blog.bootstrap.admin.*`，初始化完成并登录后建议立即修改

### 反向代理

- `COMPOSE_PROFILES=http|https`
- `NGINX_ADMIN_DOMAIN`
- `NGINX_WEB_DOMAIN`
- `ALL_DOMAINS`
- `ACME_EMAIL`
- `NGINX_PROXY_CONTAINER`

### 前端与后端联通

- 管理后台默认请求同源 `/api`，由管理后台容器 Nginx 或外层 Nginx 代理到后端
- 博客前台浏览器侧默认使用相对路径，由外层 Nginx 代理 `/api` 和 `/uploads`
- 当前 Compose 不通过根 `.env` 向 `blog-admin` / `blog-web` 注入构建变量；如需改变前端构建时变量，需要调整前端各自配置或为 Compose/Dockerfile 增加 build args

## 4. 默认端口

### 完整部署

- 博客前台：`http://localhost`
- 管理后台：`http://localhost:3001`
- 后端 API：`http://localhost:8888`
- RabbitMQ 管理：`http://localhost:15672`

说明：

- `http://localhost` 是外层 Nginx 入口，推荐作为完整 Docker 模式的前台入口
- `BLOG_WEB_PUBLISHED_PORT` 只是 `blog-web` 容器直连端口，不建议作为前台主入口
- 如果 `80` 端口被占用，需要释放端口，或把 [docker-compose.yml](../../docker-compose.yml) 中 `nginx` 服务的 `"80:80"` 改成其他宿主机端口，例如 `"8080:80"`

### 本地开发

- 管理后台：`http://localhost:3000`
- 博客前台：`http://localhost:3001`
- 后端 API：`http://localhost:8888`
- 监控端点：`http://localhost:8081/actuator`

## 5. 发布前检查

- `.env` 已从 `.env.example` 复制并按环境修改
- 完整 Docker 部署使用 Compose 中固定的 `docker` profile
- 管理员初始化账号已按生产要求修改，不再使用 profile YAML 默认密码
- 如启用邮件、地图、OSS、安全 Token、搜索或 CORS 等环境变量，确认这些变量已注入 `blog-service` 容器
- `COMPOSE_PROFILES` 已按 HTTP / HTTPS 场景设置
- 外部域名与 Nginx 域名变量一致
- 生产环境不暴露不必要端口

## 6. 常用命令

```bash
docker compose up -d
docker compose ps
docker compose logs -f blog-service
docker compose down
```

## 7. 说明

- 当前 Elasticsearch 在编排中默认禁用，如需启用，需要同时补齐服务配置和搜索开关
- 后端 Docker 运行时配置以 [application-docker.yml](../../blog-service/src/main/resources/application-docker.yml) 为准
- `docker-compose.dev.yml` 的后端容器也使用 `docker` profile；本机 `dev` profile 只用于直接运行 `blog-service`
- 历史的服务级部署说明已合并到本文档，不再维护多份 Docker 指南
