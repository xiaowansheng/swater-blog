<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.modules.system.role.mapper.RoleMenuMapper">
    <delete id="deleteByRoleId">
        DELETE FROM role_menu WHERE role_id = #{roleId}
    </delete>

    <delete id="deleteByMenuId">
        DELETE FROM role_menu WHERE menu_id = #{menuId}
    </delete>

    <select id="selectMenuIdsByRoleId" resultType="java.lang.Long">
        SELECT menu_id FROM role_menu WHERE role_id = #{roleId}
    </select>

    <select id="selectMenuIdsByRoleIds" resultType="java.lang.Long">
        SELECT DISTINCT menu_id FROM role_menu
        WHERE role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </select>
</mapper>

