# nginx-proxy 配置

# 上传文件最大大小（不配置 nginx 默认最大 1MB）
# 与后端 multipart 限制（10MB）对齐，留出请求开销余量
client_max_body_size 12m;

# 如果你有大文件上传/慢网，可顺便加：
client_body_timeout 120s;
proxy_read_timeout 300s;
proxy_send_timeout 300s;
